/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,

  // Uncomment if automatic file change detection is not working
  // webpackDevMiddleware: config => {
  //   config.watchOptions.poll = 300;
  //   return config
  // }
};

module.exports = nextConfig;
